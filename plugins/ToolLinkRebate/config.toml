[basic]
enable = true     # 总开关，设为false将完全禁用插件所有转发功能
priority = 99     # 插件优先级，值越小优先级越高

appkey = "2a77607fa088417a8fcb257d1fbb3088"  # 折淘客的对接秘钥appkey
sid = "185073"    # 折淘客的sid参数
union_id = "2036757202"  # 京东联盟ID
pid = "mm_375930105_2518750017_111777500003"  # 淘宝联盟pid
send_message_on_success = true  # 基础转链成功后是否发送消息

# 群组/用户 控制模式
# 可选值:
#   "all":       允许所有群聊和私聊 (默认)。
#   "whitelist": 仅允许在下方 `group_list` 名单中指定的群聊或用户。
#   "blacklist": 允许所有群聊和私聊，但会排除下方 `group_list` 名单中指定的群聊或用户。
group_mode = "whitelist"

group_list = [
    "50144964587@chatroom", # 转链群
    "wxid_l4u1u9bgq5u022"
]


# 线报监听配置
[xianbao]
enable = true                             # 是否启用线报监听功能
interval = 30                             # 线报监听间隔（秒）
keywords = ["得物", "锝物", "鍀物"]        # 线报关键词列表
receivers = [
#    "48829307931@chatroom",        # 得物禁言群
    "52620932598@chatroom",        # 得物测试群
]
filter_keywords = ["楼上"]
data_retention_minutes = 180                   # 线报数据保留时间，超过这个时间的数据将被自动清理
xianbao_push_message = true            # 是否推送线报消息
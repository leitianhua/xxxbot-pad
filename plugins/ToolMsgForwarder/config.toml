[ToolMsgForwarder]
# 全局设置
enable = true     # 总开关，设为false将完全禁用插件所有转发功能
priority = 99     # 插件优先级，值越小优先级越高

# ================ 转链功能配置 ================
[ToolMsgForwarder.rebate]
enable = true     # 是否启用转链功能
appkey = "2a77607fa088417a8fcb257d1fbb3088"  # 折淘客的对接秘钥appkey
sid = "185073"    # 折淘客的sid参数
union_id = "2036757202"  # 京东联盟ID
pid = "mm_375930105_2518750017_111777500003"  # 淘宝联盟pid，格式为mm_xxx_xxx_xxx

# ==================== 统一规则配置 ====================
[[ToolMsgForwarder.rules]]
enabled = true
name = "转发合并"
from_wxid = "47410316718@chatroom"          # 转发开始
# 不设置listen_specific_senders_in_group则监听群内所有人
to_wxids = ["45269739397@chatroom"]          # 转发结束
msg_types = ["text", "image", "video","file"]
enable_rebate = false                       # 启用转链功能（对文本消息生效）

# 闪购免单码
[[ToolMsgForwarder.rules]]
enabled = true
name = "闪购免单码-小梨"
from_wxid = "38832367307@chatroom"          # 小梨交流群
listen_specific_senders_in_group = [
    "25984985928202103@openim" # 闪购发码人
]  
text_filter_keywords = ["^[0-9A-Z]{6}$"]
to_wxids = ["43353238030@chatroom"]          # 转发目标
msg_types = ["text"]  # 处理所有类型消息
enable_rebate = false                      # 禁用转链功能

# 闪购免单码
[[ToolMsgForwarder.rules]]
enabled = true
name = "闪购免单码-酷儿"
from_wxid = "44611232930@chatroom"          # 酷儿
listen_specific_senders_in_group = [
    "wxid_w48lc66dv2cs12" # 酷儿
]
text_filter_keywords = ["^[0-9A-Z]{6}$"]
to_wxids = ["43353238030@chatroom"]          # 转发目标
msg_types = ["text"]  # 处理所有类型消息
enable_rebate = false                      # 禁用转链功能



# 相纸监听
[[ToolMsgForwarder.rules]]
enabled = true
name = "相纸监听 小梨S65"
#from_wxid = "34789752653@chatroom"  # 小梨S65
from_wxid = "45269739397@chatroom" # 转发结束群
to_wxids = [
#    "47322591751@chatroom" # 卷卷群
    "45269739397@chatroom" # 转发结束群
]
xml_filter_title = [".*货物.*"]
#xml_filter_des = ["^[0-9A-Z]{6}$"]
xml_filter_url = [".*jd.*"]
msg_types = ["xml"]  # 处理所有类型消息
enable_rebate = true
[ManagePlugin]
# 全局优先级设置 (0-99)，值越高优先级越高
priority = 999

[ManagePlugin.commands]
load_plugin = [ "c加载"]
load_all_plugins = ["c加载所有插件"]
unload_plugin = ["c卸载插件"]
unload_all_plugins = ["c卸载所有插件"]
reload_plugin = ["c重载"]
reload_all_plugins = ["c重载所有插件"]
list_plugins = ["c插件列表"]
plugin_info = ["c插件信息"]
